# 들어가며 — 연구용 AI 하네스

AI는 큰 저장소에서 관련 파일을 찾고, 긴 로그에서 원인 후보를 좁히며, 논문의 방법을 prototype code로 옮기는 일을 빠르게 처리한다. 그러나 작업 맥락을 놓치거나 확인하지 않은 내용을 단정하기도 한다.

그럴 때 우리는 요청을 고쳐 가며 답을 끌어낸다. 모델을 다그치기도 하고 달래기도 한다. 그렇게 얻은 답은 그때 한 번 쓰고 사라진다. 긴 연구 과정을 매번 이런 대화에만 기댈 수는 없다. 이 글은 작업 맥락과 검증 절차를 대화 대신 파일로 남기는 방법을 다룬다.

## 비싼 구독 모델을 쓰면 해결되지 않나?

이 글의 초고를 쓸 때 우리 연구실은 월 200 USD 구독을 6개월째 쓰고 있었다. 성능이 좋은 모델은 분명 도움이 된다. 다만 그 도움의 크기가 늘 같지는 않았다.

같지 않은 까닭은 우리가 모델의 동작 조건을 통제하지 못한다는 데 있다. 상용 구독은 폐쇄형 서비스다. 실제 parameter 수가 얼마인지, 요청이 어느 backend로 가는지, 어떤 system prompt가 앞에 붙는지를 공급자는 제한적으로만 공개한다.

공개된 사례가 하나 있다. Anthropic이 [2026년 4월 Claude Code postmortem](https://www.anthropic.com/engineering/april-23-postmortem)에서 그달의 품질 저하 보고를 세 원인으로 갈랐다. 모델이 한 요청에 얼마나 생각할지 정하는 값을 바꾼 것이 하나, 그 생각한 내용을 다음 요청에 이어 붙이는 캐시에 버그가 있던 것이 둘, 답을 짧게 내도록 유도한 system prompt가 셋이다. 셋 다 쓰는 쪽에서는 그날 답이 왜 달라졌는지 알 방법이 없었다.

이런 변경을 우리가 막을 수는 없다. 대신 모델 바깥에, 우리가 통제하는 자리에 작업 조건을 마련해 둘 수는 있다. 이 글이 다루는 것이 그 자리다.

변경 가운데 일부는 공급자 문서에 버전과 수치로 공개된다. 버전이 올라가면 그 수치도 함께 바뀌므로, 이런 값은 본문에 두지 않고 확인한 날짜와 함께 부록의 도구 문서 요약에 모아 두었다.

## 최신 모델을 자체 서버에서 돌리면 어떤가?

성능이 좋은 모델을 자체 서버에 올리면 공급자의 routing이나 quota 변화에는 덜 흔들린다. 그렇다고 조건이 다 손에 들어오지는 않는다.

상용 서비스의 품질은 모델 가중치 하나에서 나오지 않기 때문이다. 어느 모델을 고르는지, system prompt에 무엇을 넣는지, 긴 대화를 어떻게 줄이는지, 무엇을 캐시에 남기는지, 어떤 도구를 부르는지, 어떤 자료를 찾아 붙이는지가 모두 답을 바꾼다. 가중치를 우리 서버에 올려도 이 항목들은 여전히 우리가 정해야 한다.

그 가운데 자료를 찾아 붙이는 일은 연구가 오래 다뤄 온 문제다. 모델이 자기가 외운 것만 쓰지 않고 바깥 자료를 끌어와 쓰게 만드는 방법을 여섯 편이 차례로 밀어 놓았다. [REALM](https://arxiv.org/abs/2002.08909)은 자료를 찾아오는 부분을 사전 학습 때부터 함께 학습시켰다. [DPR](https://arxiv.org/abs/2004.04906)은 질문과 문서를 같은 공간의 벡터로 바꿔, 가까운 것을 찾는 방식으로 검색을 세웠다. [RAG](https://arxiv.org/abs/2005.11401)는 그렇게 찾아온 문서를 생성 모델에 붙여, 사실 오류와 출처 추적과 지식 갱신 문제를 함께 줄이려 했다. [FiD](https://arxiv.org/abs/2007.01282)는 찾아온 문서를 여러 편 한꺼번에 읽히는 쪽으로 확장했고, [RETRO](https://arxiv.org/abs/2112.04426)는 그 자료 창고를 대규모 텍스트 데이터베이스까지 키웠으며, [Atlas](https://arxiv.org/abs/2208.03299)는 예시가 몇 개뿐인 상황에서도 이 방식이 통한다는 것을 보였다.

여섯 편이 공통으로 말하는 것은 하나다. 답의 품질이 모델 안에 무엇이 들었는지만으로 정해지지 않고, 바깥에서 무엇을 얼마나 정확히 끌어오는지에도 달려 있다는 것이다.

끌어온 자료를 모델이 읽는 곳은 context window다. 여기 넣은 토큰이 늘수록 모델이 그 안의 내용을 정확히 되짚는 능력은 오히려 떨어진다. Anthropic의 [Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)가 이 현상을 `context rot`이라 불렀다.

떨어지는 까닭은 모델이 문장을 읽는 방식에 있다. 모델은 토큰마다 다른 모든 토큰을 견주며 어디에 주목할지 정한다. 토큰이 n개면 견줄 쌍이 n²개다. 창이 커질수록 주목할 힘이 얇게 퍼진다는 뜻이고, 같은 글이 이것을 `attention budget`이라 불렀다. 그래서 창에는 원하는 결과가 나올 만한 가장 작은 묶음만 넣고, 그 안에 들어갈 것은 신호가 분명한 자료여야 한다.

연구실에서 할 일은 그래서 web-scale index를 만드는 쪽이 아니다. 먼저 자료의 경계를 정하는 쪽이다. 어느 논문 PDF와 code path와 실험 로그와 dataset convention과 reviewer comment를 어떤 단위로 나눌지 정하고, 질문마다 찾아볼 자료를 그 단위에 연결한다.

경계를 긋는 일과 그 안에서 손을 움직이는 일은 서로 다르다. Anthropic이 [2026년 Claude Code 사용 분석](https://www.anthropic.com/research/claude-code-expertise)에서 실제 세션 약 40만 개를 놓고 이 분업을 관찰했다. 무엇을 할지와 어디까지 하면 끝인지는 사용자가 정했고, 어느 파일을 고치고 어떤 명령을 돌릴지는 Claude가 맡았다.

이 분업을 매번 즉석에서 다시 정하지 않으려면 에이전트가 따라갈 작업 지침이 있어야 한다. 이 글의 절차는 그 지침을 저장소의 `AGENTS.md`와 project memory와 실험 기록과 주장·근거 표로 옮겨 적는 방법이다.

## 무엇을 남겨야 하는가?

요청을 자세히 설명하고, 잘못을 지적하고, 빠진 맥락을 보충하면 답이 나아질 때가 있다. 다만 이 방식은 사용자가 매번 상황을 기억하고 같은 설명을 반복해야 한다. 오래 이어지는 작업에서는 버티지 못한다.

연구가 그런 작업이다. 실험은 며칠씩 이어지고 원고는 여러 번 바뀐다. 심사 의견에 답하려면 몇 달 전 결과와 지금 코드 상태를 함께 열어 봐야 한다. 이럴 때 필요한 것은 더 나은 대화가 아니라, AI의 오류를 나중에 추적하고 다시 검증할 수 있는 작업 구조다.

그 구조의 재료는 단순하다. 읽은 파일과 실행한 명령, 그리고 판단의 근거가 된 결과다. 이것을 남겨 두면 모델이 바뀌어도 세션이 끊겨도 작업을 이어 갈 수 있다. 모델의 성능에 따라 탐색 속도와 정리 품질은 달라지지만, 같은 파일과 로그를 보며 같은 절차를 따를 수 있다는 것은 변하지 않는다.

이어지는 세 부에서 그 구조를 하나씩 만든다. 부마다 만든 것은 파일로 남고, 다음 연구를 시작할 때 사람과 에이전트가 그 파일을 함께 읽는다. 사람은 목표와 공개·비공개 경계와 검증 기준을 정하고, 에이전트는 그 기준을 파일과 절차로 옮긴다.
