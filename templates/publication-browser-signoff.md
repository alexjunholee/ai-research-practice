# Publication Browser Sign-Off

## Publication Target

```text
Public URL:
Checked at:
Checked by:
Local bundle checksum file:
Final gate command:
Final gate result:
Receipt file:
```

## Automated Gate

```text
Command:
Exit status:
Remote checksum matched local dist:
Source-only paths returned 404:
Render smoke result:
```

## Browser Matrix

| View | Check | Result |
|---|---|---|
| Desktop first screen | Public title is visible | |
| Desktop navigation | Sidebar shows quickstart, terms, 8 chapters, deploy, publish, Sources | |
| Desktop content | `guide.html` opens without 404 | |
| Mobile first screen | Title and opening copy fit the viewport | |
| Mobile tables | Tables scroll or wrap without hiding text | |
| External links | External repo and source links open in a new tab | |
| Template links | Template names match files in the public bundle | |
| Example workspace | Example AGENTS and note files open | |
| Checksum files | `BUNDLE_MANIFEST.txt` and `BUNDLE_SHA256SUMS.txt` open from the public root | |
| First workspace preflight | Quickstart shows first-run checks and `artifacts/first-ai-session-message.txt` | |
| Codex porting acceptance | Ch.8 or the checklist shows the Codex porting acceptance gate | |

## Public Boundary

```text
Private paths visible:
Internal notes visible:
Raw conversation text visible:
Source-only script paths visible:
```

## Sign-Off

```text
Current evidence permits:
Current evidence forbids:
Promotion requires:
Publication claim allowed:
Remaining risk:
```
